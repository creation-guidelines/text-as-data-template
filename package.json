{
  "name": "text-as-data-template",
  "private": true,
  "description": "Commit-linting config only; this repo's content is data + SQL, not JS.",
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2"
  }
}
